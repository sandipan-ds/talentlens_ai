"""Capture evidence for each evaluation result."""

from typing import Dict, Any


def build_evidence(profile: Dict[str, Any], item: str) -> Dict[str, Any]:
    """Build evidence for a scored item."""
    return {}
