"""Load weight templates for job roles."""

from pathlib import Path
from typing import Dict, Any


def load_weight_template(path: Path) -> Dict[str, Any]:
    """Load an employer weight template from a file."""
    return {}
